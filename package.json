{
  "name": "jolimoi",
  "private": true,
  "repository": {
    "url": "https://github.com/JohnMurdok/jolimoi"
  },
  "scripts": {
    "lint": "npx lerna run lint --parallel",
    "prepare": "npx husky install",
    "start": "npx lerna run start --parallel",
    "test": "npx lerna run test --parallel"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lerna": "^5.1.4"
  }
}
